import 'package:flutter/material.dart';

class BicycleAdmin extends StatelessWidget {
  const BicycleAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
